---
title: Loop Engineering Turn Foundation Task List
description: Loop Engineering turn foundation scope の開始時点のタスク一覧。
navigation:
  icon: i-lucide-list-checks
---

# Loop Engineering Turn Foundation Task List

この task list は、Loop Engineering Implementation Roadmap の開始時点で実装する task だけを含みます。将来 scope の詳細 task は、対象 scope を開始する時点で追加します。

## Tasks

| ID | Title | Primary Area | Status |
| --- | --- | --- | --- |
| LE-01 | Task Schema and Status Discipline | `docs/content/delivery/`, `docs/content/maintenance/` | `resolved` |
| LE-02 | Loop State Separation | `STATE.md`, `.loop/`, `scripts/loop-harness.mjs` | `planned` |
| LE-03 | Harness Brief Generation | `scripts/loop-harness.mjs`, `package.json`, `docs/content/operations/` | `planned` |

## Progress

| ID | Status | Progress | Last Evidence | Next Action |
| --- | --- | --- | --- | --- |
| LE-01 | `resolved` | Task schema, status discipline, autonomy discipline, pre-implementation schema gate, `SKILL.md` stop rules, Harness validation commands, package scripts, and LE-01/LE-02/LE-03 schema fields are implemented. Acceptance Criteria are met. | `node --check scripts/loop-harness.mjs`, `pnpm loop:validate-task LE-01`, `pnpm loop:validate-tasks`, `pnpm --dir docs typecheck` passed on 2026-07-05. | None. Use LE-02 as the next planned task. |
| LE-02 | `planned` | Not started. | `pending` | Approve before implementation. |
| LE-03 | `planned` | Not started. | `pending` | Start after LE-02 or explicit approval. |

## LE-01 Acceptance Criteria Check

| Acceptance Criteria | Result | Evidence |
| --- | --- | --- |
| Required task schema fields are defined in docs. | Met | `docs/content/delivery/3.implementation-flow.md` defines `id`, `status`, `autonomy`, `source_of_truth`, `primary_area`, `acceptance_criteria`, `verification_commands`, `handoff_conditions`, and `last_evidence`. |
| Required fields include the specified minimum set. | Met | The schema table and Harness `requiredTaskFields` use the required field set. |
| Status values and meanings are defined. | Met | `planned`, `approved`, `in_progress`, `verifying`, `resolved`, and `blocked` are documented. |
| Autonomy values and meanings are defined. | Met | `manual`, `assisted`, and `autonomous` are documented. |
| `verification_commands` and `handoff_conditions` writing rules are defined. | Met | Implementation Flow defines YAML list usage, exact command rules, and concrete handoff condition rules. |
| `SKILL.md` requires schema and approved target task reading before implementation. | Met | `SKILL.md` includes pre-implementation schema and approved target task rules. |
| `SKILL.md` stops implementation when required fields are missing. | Met | `SKILL.md` includes required field stop rules. |
| Implementation Flow has a pre-implementation schema gate. | Met | Implementation Flow stops on missing fields, invalid status/autonomy, non-actionable status, and missing verification or handoff fields. |
| Harness validates task Markdown schema. | Met | `scripts/loop-harness.mjs` validates required fields, status, autonomy, `source_of_truth`, `verification_commands`, and `handoff_conditions`. |
| `package.json` exposes validation commands. | Met | `loop:validate-task` and `loop:validate-tasks` scripts are present. |
| Verifier checklist includes schema, status, autonomy, primary area, verification commands, and handoff conditions. | Met | LE-01 task Verification section lists these verifier confirmations. |
| This scope's task pages match the schema. | Met | LE-01, LE-02, and LE-03 include schema frontmatter and pass `pnpm loop:validate-tasks`. |
| No future scope detailed tasks were added. | Met | Only the existing LE-01, LE-02, and LE-03 task pages were updated. |

## Deferred Work

The following roadmap items are intentionally deferred until this scope has verifier evidence:

- Active Loop Dashboard.
- GitHub Actions turn execution.
- Low-risk automatic PR preparation.
- L3 autonomous operation.

## Execution Rule

AI Agent は、対象 task の Markdown、Current Scope、Implementation Flow、この scope の overview と acceptance criteria を読んでから実装します。task にない作業を始める場合は、先に task list または scope overview を更新します。
